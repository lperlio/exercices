// Création d'une fonction avec parametre optionnel
/*const message = function (texte = 'texte par defaut') {
  alert(texte);
};
message('Avec parametre');*/

// Création d'une fonction avec une écriture flèchée

const somme = (a, b) => {
  alert('resultat : ' + (a + b));
};
somme(20, 1);
